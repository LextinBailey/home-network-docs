# DNS Change Log

DNS change: not implemented.

Cloudflare's 1.1.1.1 was considered as a replacement for the ISP default DNS. 
Benefits would include improved privacy and potentially faster resolution. 
Change was not made as this is a shared network and modifications to core settings require consent from the network owner.